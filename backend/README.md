# Backend status

This directory is legacy/reference code.

The current runnable product path is:

- `frontend/`
- Supabase tables, auth, storage, and RPC migrations under `supabase/`

Do not treat this backend as the active production API without a separate revival pass. If it needs to be restored, verify its database schema, garbled text content, API contracts, authentication flow, and deployment configuration against the current frontend first.
