# 新生宿舍舍友自主选择系统 — 详细项目计划书

---

## 一、系统需求分析

### 1.1 系统用户角色与权限边界

| 角色 | 权限范围 | 操作边界 |
|------|----------|----------|
| **学生（新生）** | 注册/登录、填写个人信息、完成偏好问卷、查看匹配推荐、搜索与筛选舍友、发送/接收/确认舍友邀请、查看分配结果、提交异议申请 | 仅可操作本人数据，不可查看他人未公开的敏感信息 |
| **系统管理员** | 用户管理（增删改查、批量导入）、问卷模板配置、匹配算法参数调节、宿舍资源管理、系统配置、日志审计、数据统计与报表导出 | 全局管理权限，但敏感数据脱敏展示，操作留痕 |
| **院系辅导员** | 查看本学院学生匹配进度、查看本学院分配结果、处理本学院学生异议、向管理员反馈问题 | 仅限本学院范围，只读为主，异议处理为操作权限 |
| **宿舍管理处** | 宿舍楼/房间资源管理与配置、分配规则设定（如男女分区、院系集中等）、最终分配结果确认与导出、入住统计 | 资源管理权限，不可查看学生详细偏好数据（脱敏） |
| **校级管理员** | 跨院系统计报表、全局配置审核、系统安全审计 | 全局只读+审批权限，不可直接修改学生数据 |

#### 权限控制机制

```
┌─────────────────────────────────────────────────────┐
│                  RBAC 权限模型                        │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐         │
│  │  角色(R)  │──▶│ 权限(P)  │──▶│ 资源(Res) │         │
│  └──────────┘   └──────────┘   └──────────┘         │
│       │                                            │
│  ┌────┴────┐                                       │
│  │ 用户(U)  │── 多角色关联                           │
│  └─────────┘                                       │
└─────────────────────────────────────────────────────┘
```

- 采用 **JWT + RBAC** 双重认证授权机制
- 所有API调用须经过权限中间件校验
- 敏感操作（批量导出、参数修改等）需二次验证

---

### 1.2 功能模块详细列表

#### 模块一：用户注册与身份验证系统

| 子功能 | 描述 |
|--------|------|
| 统一身份认证对接 | 对接学校CAS/LDAP/学工系统，支持学号+初始密码登录 |
| 多因素认证 | 支持短信验证码/邮箱验证码/微信扫码等多种验证方式 |
| 新生邀请码机制 | 管理员生成一次性邀请码，确保只有本校新生可注册 |
| 密码安全策略 | 强密码校验、密码有效期、登录失败锁定（5次/15分钟） |
| 会话管理 | JWT Token + Refresh Token，支持单设备登录/多设备登录配置 |

#### 模块二：个人信息管理模块

| 子功能 | 描述 |
|--------|------|
| 基础信息 | 姓名、学号、性别、学院、专业、班级、生源地、联系方式 |
| 个人照片上传 | 支持证件照/生活照，需经过内容审核 |
| 个人简介 | 自由文本（150字内），支持Markdown纯文本 |
| 信息可见性控制 | 学生可自行设置各字段对匹配推荐的可见范围 |
| 信息变更审核 | 关键字段变更需管理员审核 |

#### 模块三：多维度偏好调查问卷模块

问卷设计遵循 **心理学与社会学量表设计规范**，采用 Likert 5级/7级量表与选择题相结合的方式。

**核心维度设计：**

| 维度 | 子维度 | 题目数 | 题型 |
|------|--------|--------|------|
| **生活作息** | 起床时间、就寝时间、午休习惯、作息规律性 | 5题 | 选择题+量表 |
| **卫生习惯** | 个人卫生频率、公共区域打扫意愿、物品整理习惯、对他人卫生容忍度 | 6题 | 量表 |
| **学习习惯** | 学习时间段偏好、学习环境偏好（安静/有背景音）、图书馆/宿舍学习比例、小组学习偏好 | 5题 | 混合 |
| **兴趣爱好** | 运动、音乐、游戏、阅读、影视、旅行等类别偏好 | 8题 | 多选+量表 |
| **社交偏好** | 社交频率、社交圈子大小偏好、宿舍社交期望、独处需求 | 5题 | 量表 |
| **消费观念** | 月消费水平、AA制偏好、共享物品意愿 | 4题 | 选择题 |
| **性格特征** | 内向/外向倾向、情绪稳定性、开放性、宜人性（参考Big Five简化版） | 10题 | 量表 |
| **特殊需求** | 过敏史、宗教信仰相关需求、无障碍需求等 | 3题 | 选择+填空 |

**问卷设计原则：**
- 总计约46题，预计填写时间10-15分钟
- 随机化题序以减少顺序效应
- 设置3道注意力检测题（如"本题请选C"）过滤无效问卷
- 前端分步展示（每步6-8题），带进度条
- 支持暂存草稿，随时继续填写

#### 模块四：智能匹配算法模块

```
┌─────────────────────────────────────────────────────────┐
│                    智能匹配算法架构                        │
│                                                         │
│  ┌──────────┐   ┌──────────────┐   ┌──────────────┐    │
│  │ 数据预处理 │──▶│ 特征向量化   │──▶│ 相似度计算   │    │
│  │ • 归一化  │   │ • 维度编码   │   │ • 余弦相似度  │    │
│  │ • 缺失值  │   │ • 权重分配   │   │ • 欧氏距离   │    │
│  │ • 异常检测│   │ • 降维(PCA)  │   │ • Jaccard    │    │
│  └──────────┘   └──────────────┘   └──────┬───────┘    │
│                                           │            │
│  ┌──────────┐   ┌──────────────┐   ┌──────┴───────┐    │
│  │ 结果展示  │◀──│ 排序与过滤   │◀──│ 双边匹配优化  │    │
│  │ • Top-N  │   │ • 约束条件   │   │ • Gale-      │    │
│  │ • 雷达图  │   │ • 多样性保障 │   │   Shapley   │    │
│  └──────────┘   └──────────────┘   └──────────────┘    │
└─────────────────────────────────────────────────────────┘
```

**算法详细设计：**

1. **特征工程**
   - 连续变量：Z-score标准化
   - 分类变量：One-Hot编码
   - 有序变量：整数编码后归一化
   - 多选变量：Multi-Hot编码

2. **维度权重（默认配置，可由管理员调整）**

   | 维度 | 默认权重 | 说明 |
   |------|----------|------|
   | 生活作息 | 25% | 作息冲突是最常见的宿舍矛盾来源 |
   | 卫生习惯 | 20% | 直接影响共同生活环境 |
   | 社交偏好 | 15% | 决定日常相处模式 |
   | 性格特征 | 15% | 影响长期相处和谐度 |
   | 学习习惯 | 10% | 影响学习氛围 |
   | 兴趣爱好 | 10% | 增加共同话题 |
   | 消费观念 | 5% | 影响经济往来 |

3. **综合相似度公式**

   ```
   Similarity(A,B) = Σ(Wi × Si(A,B)) / ΣWi
   
   其中：
   - Wi 为维度 i 的权重
   - Si(A,B) 为维度 i 上学生A与学生B的相似度
   - 连续变量使用余弦相似度
   - 分类变量使用 Jaccard 相似度
   ```

4. **匹配优化策略**
   - Top-20推荐列表（可根据宿舍容量调整）
   - 双向偏好匹配：A匹配到B的同时，B也匹配到A时加权加分
   - 多样性保障：确保推荐列表中包含一定比例的不同特征学生
   - 冷启动处理：新用户基于基础信息（学院、专业、生源地）做初始推荐

5. **约束条件**
   - 硬约束：同性别必须匹配（系统强制）
   - 软约束：同学院优先（管理员可配置优先级）、同班级优先

#### 模块五：室友搜索与筛选功能

| 子功能 | 描述 |
|--------|------|
| 推荐列表 | 基于算法输出Top-20推荐，展示匹配度百分比+雷达图 |
| 主动搜索 | 按学院/专业/生源地/兴趣爱好等条件筛选 |
| 关键词搜索 | 支持个人简介、兴趣爱好等文本搜索 |
| 筛选器 | 多条件组合筛选，实时更新结果 |
| 用户卡片 | 展示公开信息摘要+匹配度+共同点标签 |
| 详情查看 | 查看对方公开的完整信息（不含联系方式等敏感字段） |

#### 模块六：室友邀请与确认机制

```
学生A                      系统                      学生B
  │                         │                         │
  │  ①发送邀请（附言）       │                         │
  │────────────────────────▶│                         │
  │                         │  ②推送通知              │
  │                         │────────────────────────▶│
  │                         │                         │
  │                         │           ③查看邀请     │
  │                         │        （可查看A的公开信息+匹配度）
  │                         │◀────────────────────────│
  │                         │                         │
  │                         │  ④接受/拒绝/待考虑      │
  │                         │◀────────────────────────│
  │                         │                         │
  │  ⑤通知结果              │                         │
  │◀────────────────────────│                         │
  │                         │                         │
  │  ⑥双方确认后锁定配对     │                         │
  │◀───────────────────────▶│◀───────────────────────▶│
```

**关键规则：**
- 每人可同时发送最多 **5个** 待处理邀请
- 每人可同时接收最多 **10个** 待处理邀请
- 邀请 **48小时** 未处理自动失效
- 双方确认后形成锁定配对，不可单方面解除
- 配对锁定后，系统自动将两人从其他候选池中移除
- 支持"组队功能"：2人先配对后可联合邀请第3、4人

#### 模块七：宿舍分配规则配置系统

| 配置项 | 说明 |
|--------|------|
| 宿舍容量配置 | 设定每间宿舍最大人数（2/4/6人等） |
| 分配优先级规则 | 自主配对 > 系统推荐 > 随机分配 |
| 院系分区规则 | 可设定"同学院同楼层"或"混合居住"策略 |
| 特殊需求分配 | 无障碍宿舍、低楼层分配等优先处理 |
| 未完成学生处理 | 截止时间后未配对学生的自动分配策略 |
| 人工干预接口 | 管理员可手动调整个别分配结果 |

#### 模块八：结果公示与异议处理模块

| 子功能 | 描述 |
|--------|------|
| 分阶段公示 | 预分配结果→公示期（3天）→正式结果 |
| 学生确认 | 学生对预分配结果进行"确认"或"申诉" |
| 异议提交 | 在线填写申诉原因+支持附件上传 |
| 多级处理流程 | 辅导员初审→宿舍管理处复核→管理员终审 |
| 处理反馈 | 申诉处理状态实时可查，结果通知推送 |
| 公告通知 | 系统公告+短信/邮件/微信通知多通道推送 |

---

### 1.3 数据安全与隐私保护策略

#### 1.3.1 数据分类与保护级别

| 数据级别 | 数据内容 | 保护措施 |
|----------|----------|----------|
| **L1-公开** | 姓名（脱敏）、学院、专业、兴趣爱好标签 | 对同校学生可见 |
| **L2-受限** | 完整姓名、性别、生源地、匹配度 | 对匹配推荐可见 |
| **L3-敏感** | 身份证号、手机号、邮箱、家庭住址 | AES-256-GCM加密存储，仅管理员经审批可查 |
| **L4-高敏** | 密码Hash、过敏史、宗教信仰 | 密码BCrypt加盐哈希、健康信息独立加密 |

#### 1.3.2 安全技术措施

| 措施 | 实现方案 |
|------|----------|
| 传输加密 | 全站HTTPS（TLS 1.3），HSTS头部 |
| 存储加密 | L3/4级数据使用AES-256-GCM加密，密钥通过KMS管理 |
| 数据库加密 | 启用TDE透明数据加密 |
| 脱敏展示 | 手机号显示138****1234，身份证号显示320***********1234 |
| 审计日志 | 所有数据访问操作记录不可篡改的审计日志 |
| 数据脱敏导出 | 报表导出时自动脱敏处理 |
| 数据删除 | 毕业生数据按《个人信息保护法》要求定期清理和匿名化 |
| WAF防护 | Web应用防火墙防护SQL注入、XSS、CSRF等攻击 |
| 访问频率限制 | API接口Ratelimit，防止爬取和暴力破解 |

#### 1.3.3 合规要求

- 遵循《个人信息保护法》《数据安全法》《网络安全等级保护2.0》
- 数据收集遵循最小必要原则，仅收集宿舍分配所需信息
- 隐私政策需用户首次登录时明确同意
- 数据存储期限：在校期间保留，毕业后3年内匿名化处理

---

## 二、技术架构设计

### 2.1 技术栈选型

#### 前端技术栈

| 层级 | 技术选型 | 版本 | 选型理由 |
|------|----------|------|----------|
| 框架 | **Vue 3** | ≥3.4.x | 渐进式框架，学习曲线平缓，适合团队快速上手 |
| 构建工具 | **Vite** | ≥5.x | 极速冷启动、HMR热更新 |
| 语言 | **TypeScript** | ≥5.4.x | 类型安全，减少运行时错误 |
| UI组件库 | **Element Plus** | ≥2.7.x | 成熟的Vue3组件库，适合后台管理系统 |
| 移动端适配 | **Vant** | ≥4.x | 轻量级移动端组件库，支持新生在手机上操作 |
| 状态管理 | **Pinia** | ≥2.x | Vue3官方推荐，TypeScript友好 |
| 路由 | **Vue Router** | ≥4.x | 官方路由方案 |
| HTTP客户端 | **Axios** | ≥1.7.x | 拦截器支持、请求取消、跨平台兼容 |
| 图表可视化 | **ECharts** | ≥5.x | 匹配雷达图、数据统计大屏 |
| CSS方案 | **UnoCSS** | ≥0.60.x | 原子化CSS，按需生成，构建速度快 |

#### 后端技术栈

| 层级 | 技术选型 | 版本 | 选型理由 |
|------|----------|------|----------|
| 语言 | **Java 17 LTS** | 17 | 长期支持版本，生态成熟，教育行业主流 |
| 框架 | **Spring Boot 3** | ≥3.2.x | 企业级微服务框架，生态完善 |
| 安全框架 | **Spring Security 6** | ≥6.x | 认证授权一站式解决方案 |
| ORM | **MyBatis-Plus** | ≥3.5.x | 简化CRUD开发，分页插件便捷 |
| 数据库 | **MySQL 8.0** | ≥8.0.35 | 关系型数据存储，教育系统广泛使用 |
| 缓存 | **Redis 7** | ≥7.x | 会话管理、匹配结果缓存、排行榜 |
| 搜索引擎 | **Elasticsearch** | ≥8.x | 室友搜索引擎，支持全文检索与多条件筛选 |
| 消息队列 | **RocketMQ** | ≥5.x | 异步通知推送、邀请状态变更 |
| API文档 | **Knife4j (Swagger)** | ≥4.x | 自动生成API文档，支持在线调试 |
| 对象存储 | **MinIO** | latest | 兼容S3协议，存储照片、附件 |

#### 服务器环境

| 组件 | 选型 |
|------|------|
| 操作系统 | CentOS 8 Stream / Ubuntu 22.04 LTS |
| Web服务器 | Nginx 1.24+（反向代理+负载均衡） |
| 应用服务器 | 内嵌Tomcat（Spring Boot自带） |
| 容器化 | Docker 24+ + Docker Compose |
| 容器编排 | Kubernetes 1.28+（多机部署场景） |

---

### 2.2 数据库结构设计

#### ER关系图（核心实体）

```
┌──────────┐     ┌──────────────┐     ┌──────────────┐
│  student  │────▶│ survey_answer│◀────│ survey_question│
│ (学生表)  │     │ (问卷答案表)  │     │  (问卷题目表)  │
└────┬─────┘     └──────────────┘     └──────────────┘
     │
     │ 1:N
     ▼
┌──────────┐     ┌──────────────┐
│ invite   │────▶│ invite_state │
│ (邀请表)  │     │ (邀请状态日志)│
└────┬─────┘     └──────────────┘
     │
     │ N:2 (锁定配对)
     ▼
┌──────────┐     ┌──────────────┐     ┌──────────────┐
│ pairing  │────▶│ allocation   │◀────│  dormitory   │
│ (配对表)  │     │ (分配结果表)  │     │  (宿舍表)     │
└──────────┘     └──────┬───────┘     └──────────────┘
                        │
                        ▼
                 ┌──────────────┐     ┌──────────────┐
                 │  objection   │     │  audit_log   │
                 │  (异议表)     │     │  (审计日志表) │
                 └──────────────┘     └──────────────┘
```

#### 核心表结构设计

**1. student（学生表）**

```sql
CREATE TABLE student (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    student_no      VARCHAR(32)  NOT NULL UNIQUE COMMENT '学号',
    real_name       VARCHAR(50)  NOT NULL COMMENT '真实姓名',
    real_name_enc   VARCHAR(256) COMMENT '真实姓名加密存储',
    gender          TINYINT      NOT NULL COMMENT '性别: 0-女 1-男',
    college_id      BIGINT       NOT NULL COMMENT '学院ID',
    major_id        BIGINT       NOT NULL COMMENT '专业ID',
    class_name      VARCHAR(100) COMMENT '班级',
    hometown        VARCHAR(100) COMMENT '生源地',
    phone           VARCHAR(256) COMMENT '手机号-加密存储',
    email           VARCHAR(256) COMMENT '邮箱-加密存储',
    id_card_enc     VARCHAR(512) COMMENT '身份证号-加密存储',
    avatar_url      VARCHAR(512) COMMENT '头像URL',
    bio             VARCHAR(150) COMMENT '个人简介',
    survey_status   TINYINT DEFAULT 0 COMMENT '问卷状态:0-未填写 1-填写中 2-已完成',
    survey_completed_at DATETIME COMMENT '问卷完成时间',
    match_status    TINYINT DEFAULT 0 COMMENT '匹配状态:0-待匹配 1-邀请中 2-已配对 3-已分配',
    password_hash   VARCHAR(256) NOT NULL COMMENT '密码BCrypt哈希',
    status          TINYINT DEFAULT 1 COMMENT '账户状态:0-禁用 1-正常',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_college (college_id),
    INDEX idx_major (major_id),
    INDEX idx_match_status (match_status),
    INDEX idx_gender_college (gender, college_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学生表';
```

**2. survey_question（问卷题目表）**

```sql
CREATE TABLE survey_question (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    question_code   VARCHAR(32)  NOT NULL UNIQUE COMMENT '题目编码(如SLEEP_01)',
    dimension       VARCHAR(32)  NOT NULL COMMENT '所属维度',
    sub_dimension   VARCHAR(64)  COMMENT '子维度',
    question_text   VARCHAR(500) NOT NULL COMMENT '题干',
    question_type   VARCHAR(16)  NOT NULL COMMENT '题型:LIKERT5/LIKERT7/SINGLE_CHOICE/MULTI_CHOICE/TEXT',
    options_json    JSON         COMMENT '选项JSON:[{"label":"A","value":1,"text":"7:00前"}]',
    sort_order      INT DEFAULT 0 COMMENT '排序序号',
    is_required     TINYINT DEFAULT 1 COMMENT '是否必答',
    is_attention_check TINYINT DEFAULT 0 COMMENT '是否注意力检测题',
    status          TINYINT DEFAULT 1 COMMENT '状态:0-停用 1-启用',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_dimension (dimension),
    INDEX idx_status_sort (status, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='问卷题目表';
```

**3. survey_answer（问卷答案表）**

```sql
CREATE TABLE survey_answer (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    student_id      BIGINT       NOT NULL COMMENT '学生ID',
    question_id     BIGINT       NOT NULL COMMENT '题目ID',
    answer_value    VARCHAR(500) NOT NULL COMMENT '答案值(JSON或直接值)',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_student_question (student_id, question_id),
    INDEX idx_student (student_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='问卷答案表';
```

**4. match_result（匹配结果表 — 异步计算缓存）**

```sql
CREATE TABLE match_result (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    student_id      BIGINT       NOT NULL COMMENT '学生ID',
    target_id       BIGINT       NOT NULL COMMENT '匹配目标学生ID',
    total_score     DECIMAL(5,2) NOT NULL COMMENT '综合匹配度(0-100)',
    score_detail    JSON         COMMENT '各维度得分详情JSON',
    is_recommended  TINYINT DEFAULT 0 COMMENT '是否进入推荐列表',
    calculated_at   DATETIME     COMMENT '计算时间',
    UNIQUE KEY uk_pair (student_id, target_id),
    INDEX idx_student_score (student_id, total_score DESC),
    INDEX idx_target_score (target_id, total_score DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='匹配结果表';
```

**5. invite（邀请表）**

```sql
CREATE TABLE invite (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    from_student_id BIGINT       NOT NULL COMMENT '发送者ID',
    to_student_id   BIGINT       NOT NULL COMMENT '接收者ID',
    message         VARCHAR(200) COMMENT '附言',
    status          TINYINT DEFAULT 0 COMMENT '状态:0-待处理 1-已接受 2-已拒绝 3-已过期 4-已撤回',
    processed_at    DATETIME     COMMENT '处理时间',
    expires_at      DATETIME     NOT NULL COMMENT '过期时间(48h)',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_to_status (to_student_id, status),
    INDEX idx_from_status (from_student_id, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='室友邀请表';
```

**6. pairing（配对锁定表）**

```sql
CREATE TABLE pairing (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    pairing_code    VARCHAR(36)  NOT NULL UNIQUE COMMENT '配对编号UUID',
    group_size      INT          NOT NULL COMMENT '配对组人数',
    status          TINYINT DEFAULT 0 COMMENT '状态:0-组建中 1-已锁定 2-已分配',
    locked_at       DATETIME     COMMENT '锁定时间',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='配对锁定表';

CREATE TABLE pairing_member (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    pairing_id      BIGINT       NOT NULL COMMENT '配对ID',
    student_id      BIGINT       NOT NULL COMMENT '学生ID',
    is_initiator    TINYINT DEFAULT 0 COMMENT '是否发起人',
    joined_at       DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_pairing_student (pairing_id, student_id),
    INDEX idx_student (student_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='配对成员表';
```

**7. dormitory（宿舍表）**

```sql
CREATE TABLE dormitory_building (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    building_name   VARCHAR(100) NOT NULL COMMENT '宿舍楼名称',
    building_code   VARCHAR(32)  NOT NULL UNIQUE COMMENT '楼栋编码',
    gender          TINYINT      NOT NULL COMMENT '入住性别:0-女 1-男',
    floors          INT          NOT NULL COMMENT '楼层数',
    status          TINYINT DEFAULT 1 COMMENT '状态:0-停用 1-启用'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='宿舍楼表';

CREATE TABLE dormitory_room (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    building_id     BIGINT       NOT NULL COMMENT '楼栋ID',
    room_number     VARCHAR(20)  NOT NULL COMMENT '房间号',
    floor           INT          NOT NULL COMMENT '楼层',
    capacity        INT          NOT NULL COMMENT '容量(床位数)',
    occupied        INT DEFAULT 0 COMMENT '已占用数',
    room_type       VARCHAR(16) DEFAULT 'NORMAL' COMMENT '类型:NORMAL/ACCESSIBLE_无障碍/QUARANTINE_隔离',
    status          TINYINT DEFAULT 1 COMMENT '0-空闲 1-部分占用 2-已满 3-维修中',
    UNIQUE KEY uk_room (building_id, room_number),
    INDEX idx_type_status (room_type, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='宿舍房间表';
```

**8. allocation（分配结果表）**

```sql
CREATE TABLE allocation (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    student_id      BIGINT       NOT NULL UNIQUE COMMENT '学生ID(一人一床位)',
    room_id         BIGINT       NOT NULL COMMENT '房间ID',
    bed_no          TINYINT      COMMENT '床位号(1-N)',
    allocation_type VARCHAR(16)  NOT NULL COMMENT '分配方式:SELF_SELECT/ALGORITHM/RANDOM/MANUAL',
    allocation_batch VARCHAR(32) COMMENT '分配批次号',
    status          VARCHAR(16) DEFAULT 'PENDING' COMMENT 'PENDING/CONFIRMED/OBJECTION/RESOLVED',
    confirmed_by_student TINYINT DEFAULT 0 COMMENT '学生是否确认',
    confirmed_at    DATETIME     COMMENT '确认时间',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_room (room_id),
    INDEX idx_batch (allocation_batch)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='宿舍分配结果表';
```

**9. objection（异议表）**

```sql
CREATE TABLE objection (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    allocation_id   BIGINT       NOT NULL COMMENT '分配记录ID',
    student_id      BIGINT       NOT NULL COMMENT '申诉学生ID',
    reason          VARCHAR(1000) NOT NULL COMMENT '申诉理由',
    attachment_urls JSON         COMMENT '附件URL列表',
    status          VARCHAR(16) DEFAULT 'PENDING' COMMENT 'PENDING/COUNSELOR_REVIEW/DORM_REVIEW/ADMIN_REVIEW/RESOLVED/REJECTED',
    current_handler BIGINT       COMMENT '当前处理人ID',
    review_comment  VARCHAR(1000) COMMENT '审核意见',
    resolved_at     DATETIME     COMMENT '解决时间',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_student_status (student_id, status),
    INDEX idx_handler (current_handler)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='异议申诉表';
```

**10. audit_log（审计日志表）**

```sql
CREATE TABLE audit_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT       COMMENT '操作用户ID',
    username        VARCHAR(50)  NOT NULL COMMENT '用户名',
    role            VARCHAR(32)  NOT NULL COMMENT '角色',
    action          VARCHAR(64)  NOT NULL COMMENT '操作类型',
    target_type     VARCHAR(32)  COMMENT '操作目标类型',
    target_id       BIGINT       COMMENT '操作目标ID',
    detail          JSON         COMMENT '操作详情JSON',
    ip_address      VARCHAR(45)  COMMENT '客户端IP',
    user_agent      VARCHAR(500) COMMENT '客户端UA',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_time (user_id, created_at),
    INDEX idx_action (action),
    INDEX idx_target (target_type, target_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='审计日志表';
```

---

### 2.3 系统接口与数据交互流程

#### 整体架构图

```
┌──────────────────────────────────────────────────────┐
│                    客户端层                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │ Web端    │  │ 移动端H5 │  │ 微信小程序│           │
│  │ (Vue3)   │  │ (Vant)   │  │ (UniApp) │           │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘           │
│       └──────────────┼────────────┘                 │
└──────────────────────┼──────────────────────────────┘
                       │ HTTPS (TLS 1.3)
┌──────────────────────┼──────────────────────────────┐
│                 接入层                                │
│  ┌──────────────────┴─────────────────────┐          │
│  │         Nginx (反向代理+负载均衡)        │          │
│  │    • 限流 • WAF • 静态资源缓存          │          │
│  └──────────────────┬─────────────────────┘          │
└──────────────────────┼──────────────────────────────┘
                       │
┌──────────────────────┼──────────────────────────────┐
│                 网关层                                │
│  ┌──────────────────┴─────────────────────┐          │
│  │         Spring Cloud Gateway           │          │
│  │    • 统一认证 • 路由转发 • 跨域         │          │
│  └──────────────────┬─────────────────────┘          │
└──────────────────────┼──────────────────────────────┘
                       │
┌──────────────────────┼──────────────────────────────┐
│                 服务层                                │
│  ┌───────┐ ┌──────┐ ┌──────┐ ┌────────┐ ┌────────┐  │
│  │用户服务│ │问卷服务│ │匹配服务│ │分配服务 │ │通知服务│  │
│  │User   │ │Survey│ │Match │ │Allocate│ │Notify │  │
│  └───┬───┘ └──┬───┘ └──┬───┘ └───┬────┘ └───┬────┘  │
│      └────────┼────────┼─────────┼──────────┘       │
└───────────────┼────────┼─────────┼──────────────────┘
                │        │         │
┌───────────────┼────────┼─────────┼──────────────────┐
│              数据与中间件层                           │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌───────┐ ┌─────────┐  │
│  │MySQL │ │Redis │ │ES    │ │RocketMQ│ │MinIO    │  │
│  └──────┘ └──────┘ └──────┘ └───────┘ └─────────┘  │
└─────────────────────────────────────────────────────┘
```

#### 核心业务流程与接口设计

**流程1：问卷填写与提交流程**

```
学生填写问卷 → 暂存草稿 → 最终提交 → 触发匹配计算

API设计：
POST   /api/v1/survey/draft         保存草稿
GET    /api/v1/survey/draft         获取草稿
GET    /api/v1/survey/questions     获取题目列表（带进度）
POST   /api/v1/survey/submit        提交问卷（含注意力检测校验）
GET    /api/v1/survey/progress      获取填写进度
```

**流程2：匹配计算流程（异步）**

```
问卷提交 → 消息队列 → 匹配计算引擎 → 结果写入match_result表 → 通知前端可查看

API设计：
POST   /api/v1/match/trigger        触发匹配计算（提交问卷后自动触发）
GET    /api/v1/match/recommendations 获取推荐列表（分页，Top-20）
GET    /api/v1/match/detail/{id}    查看与特定学生的匹配详情
GET    /api/v1/search/roommates     主动搜索室友
```

**流程3：室友邀请流程**

```
API设计：
POST   /api/v1/invite/send          发送邀请 {targetId, message}
GET    /api/v1/invite/received      接收到的邀请列表
GET    /api/v1/invite/sent          发出的邀请列表
PUT    /api/v1/invite/{id}/accept   接受邀请
PUT    /api/v1/invite/{id}/reject   拒绝邀请
PUT    /api/v1/invite/{id}/withdraw 撤回邀请
GET    /api/v1/invite/quota         查看剩余邀请配额
```

**流程4：宿舍分配流程**

```
管理员设定截止时间 → 截止触发批量分配 → 生成预分配结果 → 公示期 → 确认/异议处理 → 正式分配

API设计（管理员端）：
POST   /api/v1/admin/allocation/deadline    设定匹配截止时间
POST   /api/v1/admin/allocation/execute     执行批量分配
GET    /api/v1/admin/allocation/result      查看分配结果
POST   /api/v1/admin/allocation/publish     发布预分配公示
POST   /api/v1/admin/allocation/finalize    确认正式分配结果

API设计（学生端）：
GET    /api/v1/allocation/my-result         查看个人分配结果
PUT    /api/v1/allocation/confirm           确认分配结果
POST   /api/v1/allocation/objection         提交异议申诉
GET    /api/v1/allocation/objection/{id}    查看申诉进度
```

---

## 三、开发实施计划

### 3.1 开发阶段与里程碑

```
项目总周期：约24周（6个月）

Week 1-3   ████████████ 阶段一：需求分析与原型设计
Week 4-13  ████████████████████████████████████ 阶段二：核心功能开发
Week 14-17 ████████████████ 阶段三：算法实现与测试
Week 18-20 ████████████ 阶段四：系统集成
Week 21-23 ████████████ 阶段五：用户验收测试
Week 24     ████ 阶段六：上线部署
```

#### 阶段一：需求分析与原型设计（Week 1-3）

| 周次 | 任务 | 交付物 | 负责人 |
|------|------|--------|--------|
| W1 | 需求调研、用户访谈、竞品分析 | 需求调研报告 | 产品经理 |
| W1-W2 | 功能清单确认、用户故事编写 | 产品需求文档(PRD) | 产品经理 |
| W2-W3 | 低保真线框图设计 | Axure/Figma原型 | UI设计师 |
| W3 | 高保真UI设计、设计评审 | 视觉设计稿 | UI设计师 |
| W3 | 技术方案评审、架构设计确认 | 技术设计文档 | 架构师 |

**里程碑M1：需求确认完成，原型通过评审**

#### 阶段二：核心功能开发（Week 4-13）

| 周次 | 模块 | 关键任务 |
|------|------|----------|
| W4-W5 | 基础架构搭建 | 项目脚手架初始化、CI/CD流水线搭建、数据库初始化 |
| W5-W6 | 用户系统 | 注册/登录/认证、RBAC权限、邀请码机制 |
| W7-W8 | 个人信息模块 | 信息展示/编辑、照片上传、可见性控制 |
| W8-W10 | 问卷模块 | 问卷编辑器（管理员）、问卷填写页面（学生）、草稿/提交 |
| W10-W12 | 邀请与配对 | 邀请机制、配对锁定、组队功能 |
| W12-W13 | 分配与公示 | 分配规则配置、结果展示、公示确认 |

**里程碑M2：全部功能模块开发完成，通过冒烟测试**

#### 阶段三：算法实现与测试（Week 14-17）

| 周次 | 任务 | 交付物 |
|------|------|--------|
| W14 | 特征工程实现、相似度计算核心逻辑 | 算法核心代码 |
| W15 | 匹配推荐Top-N排序、多维雷达图 | 推荐接口 |
| W15-W16 | 搜索与筛选（ES集成） | 搜索接口 |
| W16-W17 | 算法精度测试：模拟1000名学生数据验证匹配合理性 | 算法验收报告 |
| W17 | 冷启动策略、新用户推荐兜底方案 | 补充算法 |

**里程碑M3：算法验收通过，Top-10推荐中≥80%学生认为匹配合理**

#### 阶段四：系统集成（Week 18-20）

| 周次 | 任务 |
|------|------|
| W18 | 前后端联调，接口全量回归 |
| W18-W19 | 第三方系统对接（学校CAS/学工系统/短信平台） |
| W19-W20 | 全链路集成测试、数据一致性校验 |
| W20 | 性能测试、压力测试、安全渗透测试 |

**里程碑M4：系统集成完成，所有集成测试通过**

#### 阶段五：用户验收测试（Week 21-23）

| 周次 | 任务 |
|------|------|
| W21 | 面向在校生的可用性测试（招募20-30名志愿者） |
| W22 | 辅导员/管理员端操作培训与UAT |
| W22-W23 | 根据反馈修复缺陷、优化体验 |
| W23 | 最终回归测试、上线审批 |

**里程碑M5：UAT通过，所有P0/P1缺陷修复完成**

#### 阶段六：上线部署（Week 24）

| 任务 | 描述 |
|------|------|
| 生产环境准备 | 服务器采购/申请、网络配置、安全加固 |
| 数据迁移 | 新生数据导入、宿舍资源数据初始化 |
| 灰度发布 | 先选择1-2个学院试点，验证无误后全量开放 |
| 上线值守 | 核心开发+运维7×24小时值守（首周） |

**里程碑M6：系统正式上线，首个学院试点成功**

---

### 3.2 任务分配与时间节点

| 模块 | 负责人（建议角色） | 开发周期 | 交付物 |
|------|-------------------|----------|--------|
| 项目基础设施 | 架构师+DevOps | W4-W5 (2周) | 脚手架、CI/CD、环境 |
| 用户注册与认证 | 后端开发A + 前端开发A | W5-W6 (2周) | 登录注册功能 |
| 个人信息管理 | 后端开发B + 前端开发B | W7-W8 (2周) | 信息编辑/展示 |
| 问卷管理（后台） | 后端开发A + 前端开发A | W8-W9 (2周) | 问卷编辑器 |
| 问卷填写（前台） | 前端开发B | W9-W10 (2周) | 问卷填写页 |
| 智能匹配算法 | 算法工程师 | W14-W17 (4周) | 匹配服务 |
| 室友搜索与筛选 | 后端开发B + 前端开发A | W10-W11 (2周) | 搜索筛选功能 |
| 邀请与配对机制 | 后端开发A + 前端开发B | W11-W12 (2周) | 邀请配对功能 |
| 宿舍分配 | 后端开发B + 前端开发A | W12-W13 (2周) | 分配模块 |
| 异议处理 | 后端开发A + 前端开发B | W13 (1周) | 申诉流程 |
| 通知推送 | 后端开发B | W12-W13 (穿插) | 多通道推送 |
| 数据统计报表 | 前端开发A | W13-W14 (1周) | 统计大屏 |
| 测试 | 测试工程师 | W14-W23 (持续) | 测试报告 |
| UI设计 | UI设计师 | W1-W3 + 随需 | 设计稿 |

**团队配置建议（最小可行团队）：**

| 角色 | 人数 | 职责 |
|------|------|------|
| 项目经理/产品经理 | 1 | 需求管理、进度管控、干系人沟通 |
| 架构师/技术负责人 | 1 | 技术决策、代码审查、技术难点攻坚 |
| 后端开发 | 2 | 服务端功能开发、API实现、数据库优化 |
| 前端开发 | 2 | Web端/移动端适配开发 |
| 算法工程师 | 1 | 匹配算法研发与调优（可兼职） |
| UI/UX设计师 | 1 | 界面设计、交互设计、可用性测试（可兼职） |
| 测试工程师 | 1 | 测试用例编写、自动化测试、性能测试 |
| DevOps工程师 | 1 | CI/CD、部署、运维（可兼职） |

---

### 3.3 代码管理与版本控制规范

#### 分支管理策略（Git Flow简化版）

```
master/main
  │
  ├── develop ──────────────────────────────────────────
  │     │
  │     ├── feature/user-auth ──── (W5-W6)
  │     ├── feature/profile ────── (W7-W8)
  │     ├── feature/survey ─────── (W8-W10)
  │     ├── feature/matching ───── (W14-W17)
  │     ├── feature/invite ─────── (W10-W12)
  │     ├── feature/allocation ─── (W12-W13)
  │     │
  │     ├── release/v1.0.0-beta ── (W17)
  │     ├── release/v1.0.0-rc1 ─── (W20)
  │     └── release/v1.0.0 ──────── (W24)
  │
  └── hotfix/* ─────────────────── (上线后)
```

**分支命名规则：**
- 功能分支：`feature/{模块名}`
- 修复分支：`bugfix/{issue-id}-{简述}`
- 发布分支：`release/v{主版本}.{次版本}.{修订号}`
- 热修复：`hotfix/{issue-id}-{简述}`

#### 代码审查流程

```
开发者提交PR
     │
     ▼
自动化检查（CI触发）
  ├── ESLint/Checkstyle 代码规范检查
  ├── 单元测试执行
  └── 代码覆盖率检查（≥70%）
     │
     ▼ (通过)
至少1位同级开发者Review
     │
     ▼ (Approve)
合并到目标分支
     │
     ▼
自动触发构建与部署到测试环境
```

**Review要点清单：**
- [ ] 代码逻辑正确性
- [ ] 符合团队编码规范
- [ ] 关键逻辑有单元测试覆盖
- [ ] 数据库操作是否有索引支持
- [ ] 是否存在安全风险（SQL注入、XSS、敏感信息泄露）
- [ ] API接口是否符合RESTful规范
- [ ] 异常处理是否完善

#### 版本号命名规则

遵循 **语义化版本（Semantic Versioning）**：

```
v{major}.{minor}.{patch}

major: 重大功能变更或架构升级（如v1→v2）
minor: 新增功能模块（如新增异议处理模块）
patch: Bug修复、小优化（如修复登录超时问题）
```

示例：`v1.0.0` → `v1.1.0`（新增搜索功能）→ `v1.1.1`（修复搜索bug）

---

## 四、开发工具与资源清单

### 4.1 开发环境与IDE

| 工具 | 推荐选型 | 版本要求 | 用途 | 配置要点 |
|------|----------|----------|------|----------|
| Java IDE | **IntelliJ IDEA Ultimate** | 2024.1+ | 后端开发 | 安装Lombok、MyBatis-Plus、SonarLint插件 |
| 前端 IDE | **VS Code** | 1.90+ | 前端开发 | 安装Volar、ESLint、Prettier、UnoCSS插件 |
| API调试 | **Apifox / Postman** | 最新版 | 接口调试与文档 | 导入Knife4j生成的OpenAPI文档 |
| 数据库管理 | **Navicat / DBeaver** | 最新版 | 数据库设计与管理 | 配置开发/测试/生产多连接 |
| 版本控制 | **Git** | 2.44+ | 代码版本管理 | 配置.gitignore、pre-commit hooks |
| 代码托管 | **GitLab EE / Gitee** | - | 代码仓库与CI/CD | 配置分支保护规则 |
| Java版本管理 | **SDKMAN / jEnv** | - | JDK版本管理 | 统一使用JDK 17 LTS |
| Node版本管理 | **nvm-windows / fnm** | - | Node.js版本管理 | 统一使用Node 20 LTS |

### 4.2 前端UI框架与组件库

| 组件 | 选型 | 用途 |
|------|------|------|
| 组件库（PC端） | Element Plus 2.7+ | 后台管理、学生PC端 |
| 组件库（移动端） | Vant 4.x | 移动端H5适配 |
| 图表库 | ECharts 5.x | 匹配雷达图、数据统计 |
| 图标库 | @element-plus/icons-vue + Iconify | 系统图标 |
| 富文本编辑器 | Tiptap / Quill | 问卷编辑器、公告编辑 |
| 文件上传 | vue-upload-component | 照片/附件上传 |
| 日期选择 | @vueuse/core useDateFormat | 日期处理工具集 |
| 工具函数库 | @vueuse/core | Vue组合式工具集 |
| CSS工具 | UnoCSS | 原子化CSS，主题定制 |

### 4.3 后端开发框架与主要依赖

| 依赖 | GroupId/ArtifactId | 用途 |
|------|-------------------|------|
| Spring Boot | org.springframework.boot:spring-boot-starter-web | Web框架 |
| Spring Security | org.springframework.boot:spring-boot-starter-security | 安全认证 |
| Spring Data Redis | org.springframework.boot:spring-boot-starter-data-redis | Redis集成 |
| MyBatis-Plus | com.baomidou:mybatis-plus-boot-starter | ORM框架 |
| MySQL驱动 | com.mysql:mysql-connector-j | 数据库连接 |
| Knife4j | com.github.xiaoymin:knife4j-openapi3-jakarta-spring-boot-starter | API文档 |
| Hutool | cn.hutool:hutool-all | Java工具集 |
| MapStruct | org.mapstruct:mapstruct | 对象映射 |
| EasyExcel | com.alibaba:easyexcel | Excel导入导出 |
| Redisson | org.redisson:redisson-spring-boot-starter | 分布式锁 |
| RocketMQ | org.apache.rocketmq:rocketmq-spring-boot-starter | 消息队列 |
| Elasticsearch | co.elastic.clients:elasticsearch-java | 搜索引擎 |
| MinIO | io.minio:minio | 对象存储 |
| JWT | io.jsonwebtoken:jjwt-api | Token管理 |
| AWS KMS | software.amazon.awssdk:kms | 密钥管理（生产环境） |

### 4.4 测试工具

| 工具 | 用途 |
|------|------|
| JUnit 5 | 单元测试框架 |
| Mockito | Mock对象 |
| Spring Boot Test | 集成测试支持 |
| Testcontainers | Docker化集成测试（MySQL/Redis/ES容器） |
| JMeter | 性能与压力测试 |
| Selenium / Playwright | UI自动化测试 |
| OWASP ZAP | 安全漏洞扫描 |
| SonarQube | 代码质量静态分析 |

### 4.5 部署与基础设施

| 组件 | 选型 | 说明 |
|------|------|------|
| CI/CD | GitLab CI / Jenkins | 自动化构建、测试、部署流水线 |
| 容器运行时 | Docker 24+ | 应用容器化 |
| 编排（可选） | Kubernetes 1.28+ | 大规模多实例部署 |
| 反向代理 | Nginx 1.24+ | 负载均衡与静态资源 |
| 监控告警 | Prometheus + Grafana | 服务监控与告警 |
| 日志收集 | ELK (Elasticsearch+Logstash+Kibana) | 集中日志管理 |
| 配置管理 | Nacos / Apollo | 配置中心 |

### 4.6 服务器配置要求

#### 生产环境推荐配置

| 用途 | 服务器 | 配置 | 数量 |
|------|--------|------|------|
| 应用服务器 | 云服务器/物理机 | 8核CPU / 16GB RAM / 200GB SSD | 2-4台（集群） |
| 数据库服务器 | 云数据库/独立服务器 | 8核CPU / 32GB RAM / 500GB SSD | 1主1从 |
| Redis服务器 | 云Redis/独立服务器 | 4核CPU / 16GB RAM | 1主1从 |
| Elasticsearch | 云ES/独立服务器 | 8核CPU / 32GB RAM / 500GB SSD | 3节点集群 |
| 文件存储 | MinIO集群 / 云OSS | 按需扩展 | 1TB起 |
| Nginx | 与App共用/独立 | 4核CPU / 8GB RAM | 2台 |

**测试环境推荐配置：**
- 2台4核/8GB RAM/100GB SSD虚拟机
- 共享MySQL/Redis/ES（低配）

#### 网络与安全配置

| 配置项 | 要求 |
|--------|------|
| 带宽 | 公网≥100Mbps（峰值并发期） |
| 域名 | 教育网域名备案 + HTTPS证书（Let's Encrypt或商业证书） |
| 防火墙 | 仅开放80/443端口，SSH访问通过堡垒机 |
| VPN | 管理员后台访问建议通过校园VPN |
| DDoS防护 | 云服务商基础DDoS防护 |
| 等保合规 | 建议达到等保二级以上 |

---

## 五、测试与质量保障方案

### 5.1 测试策略总览

| 测试类型 | 覆盖范围 | 执行频率 | 通过标准 |
|----------|----------|----------|----------|
| 单元测试 | 所有Service/Utils层 | 每次提交 | 覆盖率≥70%，通过率100% |
| 集成测试 | API接口、数据库交互 | 每次PR | 通过率100% |
| 系统测试 | 端到端业务场景 | 每阶段末 | 通过率95%（P0/P1 100%） |
| 回归测试 | 全量功能 | 发布前 | P0/1/2缺陷清零 |
| 性能测试 | 核心高并发接口 | 集成阶段 | 见下文指标 |
| 安全测试 | 全系统 | 集成阶段 | 中高危漏洞清零 |

### 5.2 测试用例设计标准

**测试用例模板：**

```
用例编号：TC-{模块}-{序号}
用例标题：简明描述测试场景
前置条件：执行前的系统状态与数据准备
测试步骤：1.→2.→3.→
预期结果：期望的系统行为
实际结果：（执行后填写）
测试数据：使用的测试数据集
优先级：P0(阻断)/P1(严重)/P2(一般)/P3(建议)
```

**各模块测试用例数量估算：**

| 模块 | 预计用例数 | 自动化率目标 |
|------|-----------|-------------|
| 用户注册登录 | 30+ | 90% |
| 个人信息管理 | 25+ | 80% |
| 问卷填写与提交 | 40+ | 80% |
| 匹配算法 | 20+ | 95% |
| 室友搜索筛选 | 25+ | 80% |
| 邀请与配对 | 35+ | 80% |
| 宿舍分配 | 25+ | 75% |
| 异议处理 | 20+ | 75% |
| 权限与安全 | 30+ | 70% |

### 5.3 用户体验测试计划

| 项目 | 说明 |
|------|------|
| **目标用户群体** | 大二/大三在校生（模拟新生视角）20-30人；辅导员5-8人；宿舍管理员3-5人 |
| **测试方式** | 有主持的可用性测试 + 无主持的远程测试 |
| **关键场景** | ①首次登录并完成问卷 ②查看匹配推荐并搜索 ③发送/处理舍友邀请 ④组队邀请第三人 ⑤查看分配结果并提交异议 |
| **数据收集** | 任务完成率、操作耗时、错误次数、SUS可用性量表评分（目标≥75分） |
| **反馈机制** | 测试后一对一访谈 + 在线问卷 + 埋点数据分析 |

### 5.4 性能与负载测试方案

#### 关键性能指标

| 指标 | 目标值 | 测试工具 |
|------|--------|----------|
| 页面首屏加载时间（P95） | ≤2秒 | Lighthouse |
| API响应时间（P95） | ≤500ms | JMeter |
| 匹配计算（单次） | ≤3秒（异步） | JMeter |
| 并发用户数 | 支持5000同时在线 | JMeter |
| 问卷提交流量峰值 | 500 QPS（选课时段） | JMeter |
| 系统可用性 | ≥99.9%（非选课时段） | Prometheus |

#### 压力测试场景

| 场景 | 模拟条件 | 持续时间 |
|------|----------|----------|
| 问卷高峰期 | 3000并发用户同时填写提交问卷 | 30分钟 |
| 查看推荐高峰 | 5000并发用户同时刷新推荐列表 | 15分钟 |
| 邀请操作高峰 | 2000并发用户同时发送邀请 | 15分钟 |
| 混合场景 | 模拟真实用户行为分布的全链路压力 | 1小时 |

### 5.5 安全测试重点

| 测试项 | 测试方法 | 工具 |
|--------|----------|------|
| 身份认证绕过 | 尝试未登录访问API、修改JWT Token、Token重放 | Burp Suite / OWASP ZAP |
| 越权访问 | 学生A尝试访问/修改学生B的数据 | 手工测试 + 自动化 |
| SQL注入 | 所有输入框注入测试 | SQLMap |
| XSS攻击 | 富文本、个人简介等输入点 | Burp Suite |
| CSRF攻击 | 关键操作的CSRF防护验证 | Burp Suite |
| 敏感信息泄露 | 检查API响应、前端源码、日志是否含敏感信息 | 手工审计 |
| 文件上传安全 | 尝试上传WebShell、超大文件、非白名单类型 | 手工测试 |
| 密码安全 | 弱密码注册、密码传输加密、暴力破解防护 | 手工测试 |
| HTTPS配置 | SSL证书有效性、TLS版本、HSTS配置 | SSL Labs |

---

## 六、部署与维护计划

### 6.1 系统部署流程

```
1. 环境准备
   ├── 服务器采购/开通 → 操作系统安装 → 基础软件安装
   ├── 网络配置（内网IP、防火墙规则、负载均衡）
   └── 域名解析 + SSL证书配置

2. 部署前检查
   ├── 检查清单：
   │   ├── [ ] 数据库备份已创建
   │   ├── [ ] 配置文件已更新为生产环境值
   │   ├── [ ] 所有测试用例已通过
   │   ├── [ ] 数据迁移脚本已准备就绪
   │   ├── [ ] 回滚方案已确认
   │   └── [ ] 相关人员已通知
   └── 审批签字

3. 部署步骤
   ├── 停止旧版本服务（如有）
   ├── 数据库结构变更（Flyway/Liquibase迁移）
   ├── 部署新版本应用容器
   ├── 健康检查（/actuator/health）
   ├── 启用流量切换（灰度 → 全量）
   └── 监控指标确认

4. 上线后验证
   ├── 核心功能冒烟测试
   ├── 监控大盘确认（CPU、内存、QPS、错误率）
   └── 用户反馈收集

5. 版本回滚机制
   ├── 触发条件：P0级缺陷 / 严重性能问题 / 数据不一致
   ├── 回滚方式：K8s回滚到上一版本镜像 / Docker Compose切换镜像Tag
   ├── 回滚时间：≤30分钟
   └── 回滚后数据修复方案
```

### 6.2 数据备份与恢复策略

| 备份对象 | 频率 | 方式 | 保留周期 | 存储位置 |
|----------|------|------|----------|----------|
| MySQL | 每日全量 + 每小时增量(binlog) | mysqldump + binlog | 全量30天，增量7天 | 本地 + 异地OSS |
| Redis | RDB每6小时 + AOF实时 | 自动RDB快照 | 7天 | 本地 + 异地 |
| ES索引 | 每日快照 | Snapshot API | 7天 | 对象存储 |
| 文件存储(MinIO) | 每日增量 | Mirror同步 | 30天 | 异地MinIO |
| 配置文件 | 每次变更 | Git + 配置中心 | 永久 | Git仓库 |
| 审计日志 | 实时 | ELK归档 | 180天 | 冷存储 |

**恢复测试计划：**
- 每月执行一次MySQL恢复演练
- 每季度执行一次全栈灾难恢复演练
- 恢复时间目标（RTO）：≤4小时
- 恢复点目标（RPO）：≤1小时

### 6.3 后期维护与迭代方案

#### 日常维护流程

| 维护任务 | 频率 | 负责人 |
|----------|------|--------|
| 系统健康检查 | 每日 | 运维工程师 |
| 日志审查与异常分析 | 每日 | 运维工程师 |
| 安全补丁更新 | 按需（发现漏洞后48h内） | 运维+安全 |
| 数据库性能分析 | 每周 | DBA |
| 服务器资源趋势分析 | 每周 | 运维工程师 |
| 数据备份验证 | 每月 | 运维工程师 |
| 全量回归测试 | 每季度 | 测试工程师 |

#### 问题响应机制

```
问题等级          响应时间(SLA)    解决时间目标
─────────────────────────────────────────────
P0-系统不可用     15分钟内响应      4小时内解决
P1-核心功能异常   30分钟内响应      8小时内解决
P2-非核心功能异常 4小时内响应       48小时内解决
P3-体验优化建议   3个工作日内响应   下迭代纳入
```

#### 功能迭代规划

```
v1.0 — 核心功能（当前项目）
  ├── 用户注册认证
  ├── 偏好问卷
  ├── 智能匹配推荐
  ├── 室友邀请配对
  └── 宿舍分配公示

v1.1 — 体验优化（上线后1个月）
  ├── 微信小程序版本
  ├── 匹配算法参数微调
  └── 用户体验优化

v1.2 — 功能扩展（上线后2-3个月）
  ├── 宿舍生活社区（入住后互动）
  ├── 矛盾调解申请
  ├── 宿舍评分与文明宿舍评比
  └── 数据统计大屏优化

v2.0 — 平台化（次年）
  ├── 支持多校区管理
  ├── AI智能问答（常见问题答疑）
  └── 往届数据分析与预测
```

---

## 七、项目风险评估与应对措施

### 7.1 风险矩阵总览

```
影响程度
  ^
高│  数据泄露       ┌─────────────┐
  │  隐私合规       │  匹配不准   │
  │               │  性能瓶颈   │
  │               │  进度延误   │
  ├───────────────┼─────────────┤
中│ 第三方接口变动   │  需求变更   │
  │ 人员流动       │  兼容性    │
  ├───────────────┼─────────────┤
低│ 文档不全       │   UI调整   │
  │               │            │
  └───────────────┴─────────────┴──▶ 发生概率
        低              高
```

### 7.2 技术风险评估与应对

#### 风险1：匹配算法准确性不足

| 项目 | 内容 |
|------|------|
| **风险描述** | 算法推荐的学生匹配度虚高，实际入住后发现性格/习惯不合，导致大量换宿申请 |
| **发生概率** | 中 |
| **影响程度** | 高 |
| **应对策略** | ① 算法设计阶段邀请心理学/社会学专业人员参与问卷设计 ② 使用历史宿舍矛盾数据验证算法有效性 ③ 提供"匹配度解释"功能，让学生了解为什么被推荐 ④ 设置种子用户测试群，提前在小范围内验证 ⑤ 保留人工干预接口 |
| **应急预案** | 降低算法推荐权重，增加自主搜索筛选的引导；开学后1周内开启换宿申请绿色通道 |

#### 风险2：系统性能瓶颈

| 项目 | 内容 |
|------|------|
| **风险描述** | 大量新生集中在截止日期前突击填写问卷和选择舍友，导致系统响应缓慢甚至崩溃 |
| **发生概率** | 高 |
| **影响程度** | 高 |
| **应对策略** | ① 匹配计算改为异步+消息队列解耦 ② Redis预缓存推荐结果 ③ 分时段限流+排队机制 ④ 提前进行全链路压力测试 ⑤ 数据库读写分离+索引优化 ⑥ CDN加速静态资源 |
| **应急预案** | 开启限流保护，展示友好排队等待页面；紧急扩容应用服务器；必要时延长选择期限 |

#### 风险3：兼容性风险

| 项目 | 内容 |
|------|------|
| **风险描述** | 学生使用各种不同版本浏览器、不同品牌手机，页面兼容性问题影响使用体验 |
| **发生概率** | 中 |
| **影响程度** | 中 |
| **应对策略** | ① 明确支持浏览器列表（Chrome/Firefox/Safari/Edge近2个主版本） ② 移动端优先采用响应式设计+渐进增强 ③ 使用Browserslist+Autoprefixer ④ 建立兼容性测试矩阵 |
| **应急预案** | 提供"推荐使用Chrome浏览器"提示横幅；问题浏览器降级为简化版页面 |

### 7.3 数据安全风险防范

#### 风险4：个人信息泄露

| 项目 | 内容 |
|------|------|
| **风险描述** | 学生身份证号、手机号、家庭住址等敏感信息泄露 |
| **发生概率** | 低 |
| **影响程度** | 极高 |
| **应对策略** | ① 最小化数据收集原则 ② L3级以上数据强制加密存储 ③ 数据库访问白名单+审计 ④ 定期渗透测试 ⑤ 敏感信息脱敏展示 ⑥ 全员数据安全培训 |
| **应急预案** | 启动信息安全事件响应流程，24小时内通知受影响用户和监管部门 |

#### 风险5：网络攻击风险

| 项目 | 内容 |
|------|------|
| **风险描述** | DDoS攻击、Web应用攻击、中间人攻击等 |
| **发生概率** | 低 |
| **影响程度** | 高 |
| **应对策略** | ① WAF + DDoS高防 ② 全站HTTPS + HSTS ③ 定期安全扫描 ④ 最小权限原则配置服务器 ⑤ 使用堡垒机管理服务器 ⑥ 部署入侵检测系统(IDS) |
| **应急预案** | 切换高防IP；启用备用服务器；通知安全运维团队紧急处理 |

### 7.4 项目进度风险应对

#### 风险6：需求变更

| 项目 | 内容 |
|------|------|
| **风险描述** | 校方在开发过程中提出新需求或修改已有功能需求 |
| **发生概率** | 高 |
| **影响程度** | 中 |
| **应对策略** | ① 建立正式的需求变更管理流程（CCB变更委员会评审） ② 评估变更对进度/成本的影响，书面确认后再执行 ③ 优先保障P0/P1级需求，低优先级需求排入后续迭代 ④ 合同中明确需求变更条款 |
| **应急预案** | 根据变更影响评估结果，协商调整项目计划或增加资源 |

#### 风险7：资源与人员风险

| 项目 | 内容 |
|------|------|
| **风险描述** | 核心开发人员离职、校方对接人员更换、服务器资源审批延误 |
| **发生概率** | 中 |
| **影响程度** | 中 |
| **应对策略** | ① 核心模块至少2人掌握，避免单点依赖 ② 建立完善的文档和知识库 ③ 关键决策书面记录确认 ④ 提前启动服务器采购/申请流程 ⑤ 考虑云服务弹性方案作为备选 |
| **应急预案** | 启动人员替补计划；必要时引入外部技术支持；使用云服务临时替代物理服务器 |

#### 风险8：进度延误

| 项目 | 内容 |
|------|------|
| **风险描述** | 因技术难点、需求变更或人员原因导致项目延期，无法在新生开学前上线 |
| **发生概率** | 中 |
| **影响程度** | 极高 |
| **应对策略** | ① 采用敏捷开发，每2周迭代交付 ② 提前识别关键路径，优先保障 ③ 设置15%的进度缓冲 ④ 每周进度同步会，及时发现偏差 ⑤ 建立风险预警机制（进度偏差>10%时告警） |
| **应急预案** | 上线最小可行产品(MVP)覆盖核心流程，次要功能分阶段上线；申请资源加班赶工 |

---

## 附录：项目关键成功因素

1. **问卷科学性**：邀请教育心理学专家参与问卷设计，确保维度划分合理、题目信效度达标
2. **算法透明度**：匹配结果应向学生解释依据，增强信任感
3. **用户体验优先**：面向18-19岁新生，操作流程简单直观，移动端体验优秀
4. **数据安全合规**：严格遵守《个人信息保护法》，为学校规避法律风险
5. **分阶段上线**：先试点后推广，降低一次性大规模上线的风险
6. **应急预案完备**：无论技术还是业务层面，都要有完善的应急预案

---

> **文档版本**：v1.0  
> **编制日期**：2026年5月4日  
> **编制说明**：本文档为新生宿舍舍友自主选择系统的详细项目计划书，涵盖需求分析、技术架构、开发计划、测试方案、部署维护及风险评估等全部内容。各阶段具体时间节点和资源配置可根据实际院校规模与预算情况进行调整。
